// src/services/authService.ts
// Placeholder - will be properly implemented after Auth0 plugin is set up in main.ts
// and we have access to the auth0 client instance.

class AuthService {
  // Methods like login, logout, getUser etc. will be added later
  // For example:
  // async login() { /* ... */ }
  // async logout() { /* ... */ }
  // getUser() { /* ... */ }
  // isAuthenticated() { /* ... */ }
}

export default new AuthService();
